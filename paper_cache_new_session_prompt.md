# 新会话提示词（注入用）— RDMA 网关缓存论文：代码改写 + 实验

> 使用方法：把「项目记忆」（`paper_cache_memory.md`）+ 本文件，一起作为上下文注入到一个新会话，让该会话完成**最初的代码撰写 + 实验流程跑通**。
>
> ⚠️ 范围红线：**不要写论文正文**。新会话只负责：改写/新增缓存代码、写/跑实验、对生成数据做分析并产出表格和图。

---

## 一、角色与总目标

你是一名协助完成「RDMA 网关缓存方法」论文实验的系统工程师。项目背景见注入的「项目记忆」。你的任务是：

1. 在保留现有 `psn_lookup_benchmark`（时间开销实验）的基础上，**新增**一组「空间开销」实验，对比不同缓存方法的内存利用率；
2. 在缓存代码里**加入「按数据包大小动态调节内存块」的机制**（至少要能支撑空间开销实验，正文说明可后续补）；
3. 产出一套可复现的实验代码 + 数据 + 表格/图，供作者写进论文。

**论文核心卖点（贯穿所有实验）**：我们的 **PSN 映射** 方法以「空间换时间」，用 `psn % RING_BUFFER_SIZE` 直接下标实现 O(1) 存储/查找 + 按序存储与提取，把传统方法"上层整理乱序包"的负担下放到底层映射。

---

## 二、论文新思路（已按作者意图完善，请据此展开）

1. **定位**：网关缓存是现状/已有基础，我们优化的是**缓存方法（数据结构）**，不是论证"要不要网关"。
2. **核心思想（一句话）**：传统方法"底层简单、上层复杂"（底层顺序 append，上层整理乱序）；我们"以空间换时间"，底层硬件已能支撑简单映射操作，用一个简单映射把包**按序落到对应内存槽**，上层几乎不用做事。
3. **删减**：删去所有重传流程说明；删去"有无网关"的对比实验。
4. **图**：网关架构图、缓存流程图需要润色（此项偏论文写作，非你的硬性任务，可只提供数据/结构说明）。
5. **动态内存块机制**：把固定 5KB 块改为**按包大小动态调节**（见"任务 B"）。
6. **实验**：
   - 时间开销（已存在，保留并可直接引用结论）；
   - 空间开销（**新增，本任务核心**）：不同缓存方法 × 不同数据包大小 → 空间利用率；要展示"我们的方法空间利用率随包大小逐渐升高，最终与『直接叠加包』的下界差不太多"；**能加多流缓存对比更好**。
7. **不足**：正文如实说明——目前只有仿真、软实现、无实机。

---

## 三、任务清单（按顺序执行）

### 任务 A：保留现有时间开销实验（勿动/勿删）

- 目录 `260311_end_simulate/psn_lookup_benchmark_init/`（含 `psn_bench.c`、`plot_cdf.py`、`Makefile`、`README.md`）**保持原样**，作为论文「时间开销」实验的证据。
- 你只需确认它仍能 `make && ./psn_bench ... && python3 plot_cdf.py` 复现，不要重写。

### 任务 B：动态内存块机制（代码 + 说明）

把 `pkt_cache.c` 的 `cache_rdma_packet()` 从「固定 5KB」改为「按包大小动态分配」：

- 现状：`malloc(MEM_BLOCK_SIZE)`（每个包都固定 malloc 5120 字节）。
- 目标：`malloc(sizeof(struct mem_block_header) + align_up(data_len))`，或按一组尺寸档（size class，如 64/128/256/512/1024/2048/4096）向上取整，减少小包的内部碎片。
- 要求：
  - 保留 `mem_block_header` 结构不变（`data_len / recv_stamp / psn`），只改块大小策略；
  - 提供 `MEM_BLOCK_SIZE`（固定模式）与 `DYNAMIC_BLOCK`（动态模式）两种可切换的宏/开关，便于对比；
  - 如果改真实网关代码有风险，**至少**在空间开销基准（任务 C）里精确模拟这两种分配策略，正文里用"机制说明"表达即可。

### 任务 C：新增「空间开销」实验（本任务核心，交付物最多的部分）

新建一个独立单机基准程序（风格沿用 `psn_bench.c`：纯 C、单文件、Makefile、csv 输出、Python 画图），例如 `psn_space_bench/` 目录，包含 `space_bench.c` + `plot_space.py` + `Makefile` + `README.md`。

#### C.1 对比的缓存方法（与时间实验保持一致 + 一个理想下界）

| key | 方法 | 内存分配方式 |
|-----|------|-------------|
| `fifo` | FIFO 队列 | 每包 `malloc` 一个报文结构 + 指针数组 |
| `chained_hash` | 链式哈希 | 每包 `malloc` 一个节点（含 next 指针）+ payload + 桶数组 |
| `balanced_tree` | AVL 平衡树 | 每包 `malloc` 一个节点（含 left/right 指针）+ payload |
| `psn_map_fixed` | PSN 映射（固定 5KB 块，**现状**） | 每包 `malloc(5120)` + 环形数组(10240×8B) |
| `psn_map_dynamic` | PSN 映射（动态块，**新机制**） | 每包 `malloc(header + align_up(L))` + 环形数组 |
| `contiguous` | **直接叠加包（理想下界/基线）** | 把 N 个包按 `header+payload` 紧密拼接成一块，理论最小 |

> `contiguous` 代表"直接叠加包"的极限：无每包 malloc 头、无对齐碎片，是空间利用率的**上界参考**。论文要说"我们的动态方法逼近这个下界，没差太多"。

#### C.2 变量与指标

- **自变量 1**：数据包大小 `L ∈ {64, 128, 256, 512, 1024, 2048, 4096}`（字节，覆盖到 RoCEv2 MTU 附近）。
- **自变量 2**：缓存包数 `N`（可固定一个典型值，如 `N = 10240`，与时间实验对齐；多流时再乘连接数 K）。
- **因变量（核心指标）**：
  - 总分配字节 `allocated_bytes`；
  - 有效数据字节 `payload_bytes = N × L`；
  - **空间利用率 = payload_bytes / allocated_bytes**（0~1，越大越好）；
  - 辅助：每包平均分配字节 `allocated_bytes / N`（便于看出固定块浪费了多少）。
- 逐项**精确统计**每种方法真实的 malloc/数组/节点/对齐开销（含 `struct` 大小、malloc 返回的对齐、环形数组本身 10240×8B 等）。

#### C.3 输出

- `space_summary.csv`：每行 `method, packet_size, N, payload_bytes, allocated_bytes, utilization, avg_bytes_per_pkt`。
- `plot_space.py` 画 2 张图：
  1. **主图** `space_utilization.png/pdf`：横轴 = 包大小 L（log），纵轴 = 空间利用率（0~1 线性或 %）；6 条线（上面 6 个 key）。预期形态：`contiguous` 一直≈100%；`psn_map_dynamic` 从低（小包）**爬升到逼近 contiguous**（大包）；`psn_map_fixed` 一直很低（5KB 块对小包利用率极低）；`fifo/hash/tree` 因每包 malloc 头 + 对齐，中等且随 L 缓升。
  2. **辅助图** `space_absolute.png/pdf`：横轴 = L，纵轴 = 总分配字节（或每包平均分配字节），展示绝对空间消耗差异。

#### C.4（可选加分）多流缓存

- 设 K 条连接（如 K ∈ {1, 8, 64}），每条连接一个 `ring_buf`（10240×8B 固定开销）。
- 展示：随着连接数/总包数增加，PSN 映射的**每连接环形数组固定开销被摊薄**，整体空间利用率不劣化；说明"多流下空间效率依旧可观"。

### 任务 D：数据分析与交付

- 运行任务 C 的程序（在 Ubuntu 24.04 虚拟机，用户会提供环境），得到 csv 与图；
- 输出一张**可直接进论文的空间利用率对比表**（方法 × 包大小 → 利用率%）；
- 输出对结果的**解读要点**（不是论文正文，是给作者的分析摘要）：哪条曲线证明了什么、动态块相比固定块提升多少、逼近 contiguous 到什么程度；
- 与时间实验结论合并，形成"时间 + 空间"两组证据。

---

## 四、给新会话的执行约束

1. **先读**：注入的「项目记忆」+ 现有 `psn_bench.c`/`plot_cdf.py`/`README.md`，保持代码风格、命名、注释密度一致。
2. **C、单文件、无第三方依赖**：新基准程序用 gcc `-O2 -Wall -std=gnu11`，Python 只用 numpy+matplotlib。
3. **Windows 不编译**：作者在 Windows 编辑、Ubuntu 运行，代码要保证能在 Ubuntu 24.04 下 `make` 直接通过；你自己不要尝试在 Windows 编译。
4. **可复现**：每个程序带 README，写清 `make`、运行命令、参数、输出文件、图怎么画。
5. **诚实**：绝对纳秒值/绝对字节值受虚拟机影响，结论基于**相对差异与曲线形态**；在输出里注明这一点。
6. **保留**：不删除、不重写 `psn_lookup_benchmark` 已有结果与代码。

---

## 五、期望的最终交付清单

- [ ] `psn_space_bench/`（`space_bench.c` + `plot_space.py` + `Makefile` + `README.md`）
- [ ] 动态内存块机制：`pkt_cache.c` 的改动（或 `psn_space_bench` 内精确模拟 + 正文机制说明）
- [ ] `space_summary.csv`、`space_utilization.png/pdf`、`space_absolute.png/pdf`
- [ ]（可选）多流缓存结果 `space_multiflow_*.csv/png`
- [ ] 一份**实验数据解读摘要**（表格 + 每张图说明 + 论文结论要点），供作者写入论文
- [ ] 确认 `psn_lookup_benchmark` 仍可复现（时间实验证据保留）
