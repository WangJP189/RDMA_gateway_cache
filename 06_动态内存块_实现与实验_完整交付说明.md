# 06 · 动态内存块机制 — 实现与实验 完整交付说明（投放给 Claude Code）

> **本文件是唯一交付物。** 目标：让 Claude Code 在 **Ubuntu** 上，基于**旧项目（只读参考）**，
> 在**新目录**里产出**新的算法代码 + 新的模拟器 + 新的实验代码**，跑出全部数据、图（PDF 矢量）与结果说明。
>
> 阅读顺序：§0 快速上手 → §1 背景 → §2 机制规格 → §3 配置 → §4 模拟器 → §5 实验 → §6 运行 → §7 验收。

---

# 目录

- §0 交付目标与目录约定（**新目录**）
- §1 背景、术语、与旧代码的关系（旧代码**只读**）
- §2 机制完整规格（含全部伪代码）
- §3 统一配置文件（**所有预设值都集中在此**）
- §4 单机模拟器：发包 / 丢包 / 发 NAK
- §5 三个实验的完整设计
- §6 目录结构与运行命令
- §7 验收清单

---

# §0 交付目标与目录约定

## 0.1 目录约定（**最重要，先看这条**）

在目标设备上，代码库根目录下同时存在两个文件夹：

```
<repo_root>/
├── 260912_cache_ModifyExperiment/     # 【旧版本 · 只读参考】所有旧代码、旧文件都在这里
│                                       #   ★ 绝对不要修改、不要删除、不要移动其中任何文件
└── 260916_cache_AddExperiment1/       # 【★ 所有新增内容都放这里】
```

**规则**：

1. **旧目录 `260912_cache_ModifyExperiment/` 只读**。可以读取它来复用基线实现、对齐计时口径；**禁止修改**。
2. **所有新文件一律写入 `260916_cache_AddExperiment1/`** —— 新算法代码、模拟器、实验代码、CSV、图、报告，全部在它下面。
3. `260916_cache_AddExperiment1/` 必须**自包含可编译**：把需要的基线实现**复制**到它的 `baselines/` 子目录（保持语义不变、加注释标注来源），不要跨目录 `#include` 旧文件。
4. 最终交付必须能在这个新目录里 `make` 一把编译通过。

## 0.2 你要产出什么

1. **新算法**：`psn_dynblock`（本文方法）。基线 `fifo` / `chained_hash` / `balanced_tree` 从旧目录复制复用；消融 `psn_fixed` / `psn_map_static` / `psn_map_tiered_old`。
2. **单机模拟器**：发包器 + 丢包模型 + 发 NAK 器（§4）。
3. **3 个实验**（§5），一键复现。
4. **数据**：全部 CSV（schema 见 §5）。
5. **图**：PDF 矢量 + PNG 300 dpi（规范见 §5.6）。
6. **报告**：`RESULTS.md` + `ASSERTIONS.txt` + 每次运行的 `resolved_config.json`。

## 0.3 环境与五条不可违背的纪律

- **OS**：Ubuntu（实验在另一台机器上跑）。C，`gcc -O2 -Wall -std=gnu11 -lm`。出图：Python 3 + matplotlib + numpy。
- 纪律 1：**不许改旧目录任何文件**；基线实现复制过来后**语义也不许改**。
- 纪律 2：**计时口径**沿用旧项目约定 —— 批量 `rdtsc`（批大小见配置）+ TSC 标定到 ns；**每个计时循环前必须 warm-up**；RNG 用 `xorshift32`，种子固定。
- 纪律 3：**每个实验必须一键复现**，`README.md` 写明确切命令。
- 纪律 4：**所有 CSV**：ASCII 列名、逗号分隔、无 BOM。
- 纪律 5：**遇到计时口径/平台差异，先说明再动手，不要静默改动口径。**

---

# §1 背景、术语、与旧代码的关系

## 1.1 场景一句话

RDMA 在广域网里因长 RTT、丢包、乱序而性能骤降。**网关缓存**：在网关处缓存 in-flight 数据包；丢包发生时由网关**本地快速重传**（缩短重传路径），并支持**选择性重传（SR）**而非仅回退 N 帧（GBN）。

## 1.2 我们的方法一句话

**把"排序"下放到"地址计算"**：包序号（PSN）是 24-bit 空间 `ℤ_{2^24}` 上的离散索引，用确定性映射

```
i = Φ(p) = (p − p₀) mod N          （N = 环长）
```

把每个包直接落到环形数组的固定槽位。于是：**存入即有序、取出零重排、存取 O(1)**。

## 1.3 术语表（全文统一）

| 符号 | 含义 | 代码字段 |
|---|---|---|
| `N` | 环长（槽数）= 重传窗口长度 | `cfg.ring_n` |
| `p` | 包的 PSN（24-bit 回绕） | — |
| `p₀` | 映射原点 | `c->p0` |
| `i` | 槽索引，`i = (p−p₀) mod N` | `phi()` |
| `S` | **当前块大小（槽容量，字节）** —— 唯一自由旋钮 | `c->S` |
| `SC` | 尺寸档位集合 | `cfg.sc[]` |
| `stride` | 槽跨度 = `align16(HDR + S)` | `c->cur.stride` |
| `epoch` | 一个纪元 = 环被写满一圈（N 次 store） | — |
| `L_ring` | 本纪元进入环的包的最大长度 | `c->epoch_ring_max` |
| `L_ovf` | 当前溢出数组里的最大包长 | `ovf_scan_max()` |
| `ovf` | 溢出数组（兜底区） | `c->ovf[]` |
| `gen` | 世代标签（双缓冲切换用） | `meta[i].gen` |
| NAK | 接收端发来的重传请求 | — |
| GBN | 回退 N 帧：NAK 携带一段**连续** PSN 区间 | — |
| SR | 选择性重传：NAK 携带**离散** PSN 集合/bitmap | — |

## 1.4 旧代码里有什么（供复用参考）

- `psn_lookup_benchmark/`：查找时间基准（含 CDF、scaling）。
- `psn_space_bench/space_bench.c`：空间利用率。其 `psn_map_tiered` 是 **6 个并列的环**（固定开销大）——**这正是要被替换掉的旧方案**，保留为消融对照。
- `psn_behavior_bench/behavior_bench.c`：行为基准（store / retrieve / zero-reorder / malloc 计数）。
- `paper_figures/plot_figures.py`：旧出图脚本。

**复用方式**：把 `fifo` / `chained_hash` / `balanced_tree` 三个基线的实现**复制**到新目录 `baselines/`，**语义与计时口径保持不变**；新增 `*_pool` 变体（见 §5.1.1 对照 D）。

---

# §2 机制完整规格

> 每一处逻辑都已闭合，实现时严格照做。

## 2.1 三条核心不变量

| # | 不变量 | 说明 |
|---|---|---|
| **I1** | `i ≡ Φ(p) = (p − p₀) mod N`，**与 S 无关** | 改块大小只改 `stride`，**不改索引** → PSN 连续性由结构保证，无需任何合并/排序机制 |
| **I2** | `store / lookup / retrieve` 均 **O(1)** | lookup 最多 1 次额外解引用（溢出路径） |
| **I3** | 稳态下**环路径 malloc = 0** | 池预分配；溢出是少数路径，允许少量分配 |

**关键立论**：`S` 是**性能旋钮**，不是**正确性依赖**。任何 `S` 下超尺寸包都走溢出数组，取包都正确 → 自适应算法**允许不完美**。

## 2.2 数据结构

```c
enum { SLOT_FREE = 0, SLOT_IN_RING = 1, SLOT_IN_OVF = 2 };

typedef struct {              /* 16 B；与 payload 分离存放 */
    uint32_t psn;             /* 该槽当前存的 PSN —— 【正确性关键】drain 时校验用 */
    uint16_t len;
    uint8_t  flag;
    uint8_t  gen;
    uint32_t ovf_idx;
} slot_meta_t;

typedef struct {              /* 池 = 一代 */
    uint8_t *base;            /* N × stride 字节 */
    uint32_t S, stride;
    uint8_t  gen;
} pool_t;

typedef struct {
    uint32_t psn;
    uint16_t len;
    uint8_t  used, pad;
    uint8_t *blk;
} ovf_entry_t;

typedef struct {
    uint32_t N, S, p0, next_psn, window_base;
    pool_t   cur;
    pool_t   old; int has_old;
    uint32_t cur_live, old_live;
    slot_meta_t *meta;        /* N 项，堆分配 */
    ovf_entry_t *ovf;         /* cfg.ovf_cap 项 */
    uint32_t ovf_count, ovf_max_hist;
    uint32_t epoch_stores, epoch_ring_max, quiet_epochs;
    int      grow_streak, shrink_streak;
    uint64_t last_check_ns;
    uint64_t n_store, n_lookup, n_resize, n_drain, n_evict, n_drop, n_ovf_ins;
    const cfg_t *cfg;
    pthread_spinlock_t lk;
} conn_t;
```

> **⚠️ `meta` 必须与 payload 分开放**：每次检查只扫 `meta`（≈160 KB），**完全不碰 payload**（10 MB 级）。这是"每 N 个包查一次"代价可忽略的根本原因。

## 2.3 辅助函数

```c
static inline uint32_t align16(uint32_t x) { return (x + 15u) & ~15u; }
static inline uint32_t stride_of(uint32_t S) { return align16(HDR_SZ + S); }
static inline uint32_t phi(conn_t *c, uint32_t psn) { return (psn - c->p0) % c->N; }
static inline uint8_t *slot(pool_t *p, uint32_t i) { return p->base + (size_t)i * p->stride; }
static inline uint8_t *payload_of(pool_t *p, uint32_t i) { return slot(p, i) + HDR_SZ; }
static inline pool_t *pool_for(conn_t *c, uint8_t gen) {
    return (gen == c->cur.gen) ? &c->cur : &c->old;
}
static uint32_t ceil_class(const cfg_t *cf, uint32_t x) {
    for (int i = 0; i < cf->n_sc; i++) if (cf->sc[i] >= x) return cf->sc[i];
    return cf->sc[cf->n_sc - 1];      /* 超过最大档 ⇒ 永远走溢出 */
}
```

## 2.4 阶段一：建联

**在哪做**：RDMA CM 建连完成后。CM 建连报文**本来就交换了 path MTU**（∈{256,512,1024,2048,4096}），直接读。**不需要新协商协议。**

```c
void conn_init(conn_t *c, const cfg_t *cfg, uint32_t mtu_from_cm) {
    c->cfg = cfg; c->N = cfg->ring_n;
    c->S  = ceil_class(cfg, mtu_from_cm ? mtu_from_cm : cfg->fallback_mtu);
    c->p0 = 0; c->next_psn = 0; c->window_base = 0;

    c->cur.gen = 0;
    pool_alloc(&c->cur, c->N, c->S);        /* mmap N × align16(HDR+S)，不预先 touch */
    c->has_old = 0; c->cur_live = 0; c->old_live = 0;

    c->meta = calloc(c->N, sizeof(slot_meta_t));    /* 全 FREE */
    ovf_reset(c);
    reset_epoch(c);
    c->last_check_ns = now_ns();
    trace_S(c, c->S, TRACE_INIT);
}
```

**为什么初始 S = MTU 安全**：RDMA 分段保证单包不超过 path MTU（更大的消息被切成多个 MTU 包）。所以 `S = MTU` 时**理论上永不溢出**；溢出数组是给理论边界之外留的兜底。

## 2.5 阶段二：正常缓存（store）

```c
int conn_store(conn_t *c, uint32_t psn, const uint8_t *payload, uint16_t len) {
    spin_lock(&c->lk);
    uint32_t i = phi(c, psn);
    evict_occupant(c, i);                    /* ★ 见 §2.6：环形回绕即淘汰 */

    if (len <= c->S) {                       /* 正常路径：进环 */
        uint8_t *dst = payload_of(&c->cur, i);
        memcpy(dst, payload, len);
        hdr(dst)->len = len; hdr(dst)->psn = psn;
        c->meta[i] = (slot_meta_t){ .psn=psn, .len=len,
                                    .flag=SLOT_IN_RING, .gen=c->cur.gen };
        c->cur_live++;
        if (len > c->epoch_ring_max) c->epoch_ring_max = len;   /* O(1) 增量维护 */
    } else {                                 /* 溢出路径 */
        int oi = ovf_alloc(c, psn, payload, len);
        if (oi < 0) {                        /* 溢出数组满 ⇒ 紧急"长" */
            spin_unlock(&c->lk);
            conn_emergency_grow(c, len);
            spin_lock(&c->lk);
            oi = ovf_alloc(c, psn, payload, len);
            if (oi < 0) { c->n_drop++; spin_unlock(&c->lk); return R_DROPPED; }
        }
        c->meta[i] = (slot_meta_t){ .psn=psn, .len=len, .flag=SLOT_IN_OVF,
                                    .gen=c->cur.gen, .ovf_idx=(uint32_t)oi };
        if (len > c->ovf_max_hist) c->ovf_max_hist = len;
    }

    c->next_psn++; c->window_base = c->next_psn - c->N;
    c->epoch_stores++; c->n_store++;
    release_old_if_drained(c);

    if (c->epoch_stores >= c->N ||
        now_ns() - c->last_check_ns >= c->cfg->t_epoch_ns)
        check_and_maybe_resize(c);           /* 见 §2.9 */

    spin_unlock(&c->lk);
    return R_OK;
}
```

普通包成本 = 一次取模 + 一次乘法（算地址）+ 一次 memcpy。**无分配、无遍历、无排序。**

## 2.6 阶段三：清理/淘汰 —— 「环形回绕本身就是淘汰策略」

> **环长 `N` 恰好等于重传窗口长度。**
> 写 PSN = `p` 落到槽 `i = (p−p₀) mod N`；当 **`p + N`** 到来时它落到**同一个槽 `i`**，**自动覆盖 `p` 的包** —— 而此刻 `p` 已滑出重传窗口、**不可能再被 NAK**。
>
> **淘汰不是动作，是写入的副作用。没有定时器、没有 LRU、没有显式 delete、没有周期 GC。**

```c
static void evict_occupant(conn_t *c, uint32_t i) {
    slot_meta_t *m = &c->meta[i];
    if (m->flag == SLOT_FREE) return;
    if (m->flag == SLOT_IN_RING) {
        if (m->gen == c->cur.gen) c->cur_live--; else c->old_live--;
    } else {                                  /* 溢出项需【显式】回收 */
        ovf_release(c, m->ovf_idx);
    }
    m->flag = SLOT_FREE;
    c->n_evict++;
}
```

**溢出数组为什么需要显式回收**：它的下标 `ovf_idx` 是任意的，**不被索引覆盖**。所以当某个槽被新包覆盖时，顺手释放它的溢出项。**触发时机依然是"被下一次写入顶掉"，无需定时器。**

**空闲连接**：环是固定 `N×stride` 的预分配缓冲，**不增长**，所以"留着"有界安全；溢出有硬上限。**不需要 GC。**

## 2.7 阶段四：收到 NAK —— 取出与重传

| 模式 | NAK 携带什么 | 网关动作 |
|---|---|---|
| **GBN** | 一段**连续** PSN 区间 | 逐个定位并发送 |
| **SR** | 一组**离散** PSN（集合/bitmap） | 逐个定位并发送 |

```c
const uint8_t *conn_lookup(conn_t *c, uint32_t psn, uint16_t *out_len) {
    uint32_t i = phi(c, psn);
    slot_meta_t m = c->meta[i];
    if (m.flag == SLOT_FREE || m.psn != psn) return NULL;   /* 已滑出窗口 */
    c->n_lookup++;
    if (m.flag == SLOT_IN_RING) {                           /* 0 次额外解引用 */
        pool_t *p = pool_for(c, m.gen);
        *out_len = m.len; return payload_of(p, i);
    } else {                                                /* 1 次额外解引用 */
        *out_len = c->ovf[m.ovf_idx].len;
        return c->ovf[m.ovf_idx].blk;
    }
}

/* GBN：连续区间 */
int conn_retransmit_range(conn_t *c, uint32_t p_start, uint32_t L_req,
                          uint8_t *out, uint32_t out_cap) {
    uint32_t w = 0;
    for (uint32_t k = 0; k < L_req; k++) {
        uint16_t len; const uint8_t *p = conn_lookup(c, p_start + k, &len);
        if (!p) continue;
        memcpy(out + w, p, len); w += len;
        if (w >= out_cap) break;
    }
    return (int)w;
}

/* SR：离散集合 */
int conn_retransmit_set(conn_t *c, const uint32_t *psns, uint32_t n,
                        uint8_t *out, uint32_t out_cap) {
    uint32_t w = 0;
    for (uint32_t k = 0; k < n; k++) {
        uint16_t len; const uint8_t *p = conn_lookup(c, psns[k], &len);
        if (!p) continue;
        memcpy(out + w, p, len); w += len;
        if (w >= out_cap) break;
    }
    return (int)w;
}
```

**三条必须写死的语义**：

1. **取包不删除**。选择性重传会对**同一 PSN 反复 NAK**，所以取包**绝不淘汰**，只等窗口自然滑出。
2. **查不到返回 NULL**（靠 `meta[i].psn == psn` 校验），不能返回错包。
3. **GBN 是最大红利**：连续 `L_req` 个 PSN → 环上 `L_req` 个**连续槽** → 内存顺序访问、预取友好；hash/tree 是 `L_req` 次随机访存。**这一点要写进论文，并在实验一量化。**

## 2.8 阶段五：什么时候评估（"经过一段时间"）

```
epoch_stores >= N                          ← 主触发：环被写满一圈
        OR
now_ns() - last_check_ns >= T_EPOCH         ← 兜底：低速率连接（见配置）
```

**为什么"环满一圈"是主触发**：① 天然，不需额外时钟，与窗口滑动对齐；② 此刻 `meta` 恰好是"最近 N 个包"的完整快照；③ 代价可忽略（`L_ring` 是 O(1) 增量维护，检查时只再扫一遍溢出数组）；④ 只读统计量、不碰 payload、临界区几微秒，**不打扰转发**。

## 2.9 阶段六/七：缩小 与 扩大

### 2.9.1 缩小 —— 条件

| # | 条件 | 目的 |
|---|---|---|
| C1 | `ovf_count == 0` 且 `quiet_epochs >= J_QUIET` | 环境真安静才缩 |
| C2 | `L_ring <= S × SHRINK_GAMMA` | 缩得**值**才缩 |
| C3 | `target < S` 且 `target >= ceil_class(ovf_max_hist)` | 绝不缩到"最近溢出包"以下（防振荡） |
| C4 | 连续 `K_DWELL` 个纪元都满足 | 滞后防抖 |

其中 `target = max(ceil_class(L_ring), ceil_class(ovf_max_hist))`。

### 2.9.2 缩小的动作 —— **新建池 + 双世代切换，不迁移任何数据**

**直接回答"是新创建数组一一迁移，还是直接把原数组缩小？"**：

- ❌ **不能"直接把原数组缩小"**。`stride = align16(HDR + S)` 变了 ⇒ **每个槽的地址都平移**，所有数据都得挪位置。（唯一能原地不动的是"分页+页表"，但会引入一层间接寻址，破坏"地址=一次乘法"的简洁性。）
- ✅ **做法 A：新建池 + 一一迁移** —— O(N) 次 memcpy，**有停顿**。
- ✅ **做法 B（采用）：双世代切换，零迁移** —— 分配新池**只给未来的包用**；旧包**留在旧池不动**，靠 `meta[i].gen` 找到；旧池随窗口自然排空后整体释放。**O(1)，零停顿。**

> **合理性说明**：缩小场景下**做法 A 也合法**（缩的目标是 `ceil_class(L_ring)`，**所有环内包都 ≤ 新 S**，搬过去一定放得下）。A 不是错的，只是慢；选 B 为了**不停顿**。

### 2.9.3 扩大 —— 条件

| 途径 | 条件 |
|---|---|
| **常规** | 本纪元结束时 `ovf_count >= OVF_THRESH`，且**连续 `K_DWELL` 个纪元**满足 |
| **紧急** | `ovf_alloc()` 失败（溢出数组被瞬间打满）→ 立即扩 |

> 阈值取 `OVF_CAP/2`：细水长流式溢出要攒很久才触发 → **被容忍**；突发式瞬间打满 → **立刻响应**。天然区分"偶发"与"趋势"。

### 2.9.4 扩大 —— 动作：切池 + **把溢出包全部搬回环内**

```c
static void check_and_maybe_resize(conn_t *c) {
    const cfg_t *cf = c->cfg;
    uint32_t L_ring  = c->epoch_ring_max;
    uint32_t ovf_cnt = c->ovf_count;
    uint32_t L_ovf   = ovf_scan_max(c);         /* O(OVF_CAP)，~1 μs */
    uint32_t S_old   = c->S;

    c->epoch_stores = 0; c->epoch_ring_max = 0; c->last_check_ns = now_ns();
    if (ovf_cnt == 0) c->quiet_epochs++; else c->quiet_epochs = 0;
    if (c->quiet_epochs >= cf->hist_decay) c->ovf_max_hist = 0;

    /* ===== 规则 A：溢出过多 ⇒ 块太小 ⇒ 长 ===== */
    if (ovf_cnt >= cf->ovf_thresh) {
        c->grow_streak++; c->shrink_streak = 0;
        if (c->grow_streak >= cf->k_dwell) {
            uint32_t S_new = ceil_class(cf, L_ovf ? L_ovf : c->S + 1);
            if (S_new > c->S) {
                gen_switch(c, S_new);
                if (c->S == S_new) {            /* 切换真的发生了 */
                    drain_overflow(c);
                    trace_resize(c, S_old, S_new, REASON_GROW);
                }
            }
            c->grow_streak = 0;
        }
        return;
    }
    c->grow_streak = 0;

    /* ===== 规则 B：环内最大包远小于 S ⇒ 块太大 ⇒ 缩 ===== */
    if (ovf_cnt == 0 && c->quiet_epochs >= cf->j_quiet && L_ring > 0) {
        uint32_t target = ceil_class(cf, L_ring);
        if (target < ceil_class(cf, c->ovf_max_hist))
            target = ceil_class(cf, c->ovf_max_hist);
        if (target < S_old && L_ring <= (uint32_t)(S_old * cf->shrink_gamma)) {
            c->shrink_streak++;
            if (c->shrink_streak >= cf->k_dwell) {
                gen_switch(c, target);
                if (c->S == target) trace_resize(c, S_old, target, REASON_SHRINK);
                c->shrink_streak = 0;
            }
        } else c->shrink_streak = 0;
    } else c->shrink_streak = 0;
}
```

```c
static void gen_switch(conn_t *c, uint32_t S_new) {
    if (c->has_old && c->old_live > 0) return;      /* 上次旧池未排空 ⇒ 本次延后（见 §2.11-3） */
    if (c->has_old) { pool_free(&c->old); c->has_old = 0; }

    pool_t np;
    pool_alloc(&np, c->N, S_new);      /* ★ 建议锁外预分配 spare，锁内只换指针 */
    np.gen = c->cur.gen + 1;

    c->old = c->cur; c->has_old = 1; c->old_live = c->cur_live;
    c->cur = np;     c->cur_live = 0;
    c->S = S_new;
    /* meta[i].gen 不动：老包仍带老 gen，lookup 按 gen 选池 → 仍 O(1) */
}

static void release_old_if_drained(conn_t *c) {
    if (c->has_old && c->old_live == 0) { pool_free(&c->old); c->has_old = 0; }
}

static void drain_overflow(conn_t *c) {
    uint32_t moved = 0;
    for (uint32_t oi = 0; oi < c->cfg->ovf_cap; oi++) {
        ovf_entry_t *e = &c->ovf[oi];
        if (!e->used) continue;
        uint32_t i = phi(c, e->psn);
        slot_meta_t *m = &c->meta[i];
        if (m->flag == SLOT_FREE || m->psn != e->psn) { ovf_release(c, oi); continue; }
        uint8_t *dst = payload_of(&c->cur, i);
        memcpy(dst, e->blk, e->len);
        hdr(dst)->len = e->len; hdr(dst)->psn = e->psn;
        m->flag = SLOT_IN_RING; m->gen = c->cur.gen; m->len = e->len;
        c->cur_live++;
        ovf_release(c, oi);
        moved++;
    }
    c->ovf_count = 0;
    c->n_drain += moved;
}

static void conn_emergency_grow(conn_t *c, uint32_t need) {
    spin_lock(&c->lk);
    uint32_t S_new = ceil_class(c->cfg, need);
    if (S_new > c->S) {
        gen_switch(c, S_new);
        if (c->S == S_new) { drain_overflow(c); c->n_resize++; }
    }
    spin_unlock(&c->lk);
}
```

**为什么旧池一定会在下次切换前排空（逻辑闭合点）**：旧池的包都是 PSN < 切换点的；此后每来一个包 `next_psn` 前进一格，前进满 N 格时**所有旧槽都被覆盖过一遍**，旧池清空。而切换要求**连续 `K_DWELL`（≥2）个纪元**，两次切换之间至少隔 **2N 次 store > N** → 旧池总能排空 → **任一时刻最多 2 个池**。✅

## 2.10 缩块之后收到大包会怎样

```
S 缩到 1024，此时来一个 4096 B 的包：
  i = Φ(p)；4096 > 1024 ⇒ 不属于环
  → 拿一个变长块写进去
  → meta[i] = { psn, 4096, flag=IN_OVF, ovf_idx=oi }
  → lookup 看到 IN_OVF，一次解引用就拿到 → 仍 O(1)
```

**漂亮的副作用**：这个大包**没进环**，所以 `L_ring` **不受它影响** → 该统计量保持稳健。**这正是"按 max 缩"能安全成立的真正原因**：溢出数组相当于给统计量加了个滤波器。

## 2.11 边界情形

| # | 情形 | 处理 |
|---|---|---|
| 1 | 包 > `SC_MAX` | 永远走溢出；溢出满且无法 drain ⇒ 丢弃 + `n_drop++`（**不破坏正确性**） |
| 2 | 突发瞬间打满溢出 | `ovf_alloc` 失败 ⇒ **紧急扩大** |
| 3 | 上次切换的旧池还在排空，又触发切换 | **本次延后**，下一纪元重试（由 §2.9.4 时序分析知其几乎不会发生） |
| 4 | 缩完又开始溢出 | **预期行为**，即闭环 |
| 5 | 缩-长振荡 | 三重护栏：`SHRINK_GAMMA` + `J_QUIET` + `ovf_max_hist`（安静 `HIST_DECAY` 纪元后衰减） |
| 6 | 乱序到达 | **直接按 `i = Φ(p)` 落位** —— 槽位不依赖到达顺序 |
| 7 | 并发 | 每连接一把自旋锁；`meta[i].gen` 决定取池；新池锁外预分配 |
| 8 | 低速率连接 | `T_EPOCH` 兜底检查 |
| 9 | 过渡期内存 ×2 | 有界（≤2 池）+ mmap 惰性提交 |
| 10 | 空闲连接 | 环固定不增长；溢出有上限。无需 GC |

## 2.12 不变量自测清单（写入 `ASSERTIONS.txt`）

| # | 断言 |
|---|---|
| A1 | 任意 S、任意世代下 `phi(p)` 一致（随机 10^6 次） |
| A2 | `store/lookup` 为 O(1)（不随 N 增长） |
| A3 | 稳态 `malloc/store == 0`（环路径） |
| A4 | `cur_live + old_live == 环内有效槽数` |
| A5 | 任一时刻池数 ≤ 2 |
| A6 | 每次 resize 后 `S >= 所有环内包长` |
| A7 | `drain` 后 `ovf_count == 0`，且原溢出包都能 lookup 到 |
| A8 | 24-bit PSN 回绕正确 |
| A9 | 同一 PSN 反复 lookup 结果一致且**不被删除** |
| A10 | 缩块后到来的大包，lookup 能正确取到 |

---

# §3 统一配置文件（**所有预设值都在这里**）

> 要求：**机制参数 + 所有实验/负载预设值**全部集中到 `include/config.h`，每项都有解释。
> 改这一处即可全局生效。**实验三的阶段表也在里面**（见 §3.1 E 段）。

## 3.1 `include/config.h`

```c
#ifndef CFG_H
#define CFG_H
#include <stdint.h>

/* ==========================================================================
 *  动态内存块机制 & 全部实验预设值 —— 唯一配置文件
 *  论文里只用符号表示，本文件给出默认值。改这里即可全局生效。
 * ========================================================================== */

/* ==================== A. 机制参数 ==================== */

#define CFG_RING_N              10240u   /* N：环长（槽数）= 重传窗口长度。
                                          * 由 BDP 决定：需 >= 带宽时延积对应的 in-flight 包数。 */

/* 尺寸档位集合 SC（向上取整到档）。
 * 含 1536 中间档，是为压住 1280/1500 这类【非 2 幂次以太网帧长】的内部碎片：
 * 若只有 {1024,2048}，1280 B 会落到 2048 档 → 约 30% 内部碎片。 */
#define CFG_SC_LIST  { 256u, 512u, 1024u, 1536u, 2048u, 3072u, 4096u, 8192u }
#define CFG_SC_N     8

#define CFG_OVF_CAP       512u      /* 溢出数组容量（"几百个"）。硬上限，不增长。 */
#define CFG_OVF_THRESH    256u      /* = OVF_CAP/2：溢出条数 >= 此值 ⇒ 触发"扩大"。
                                     * 细水长流要攒很久才触发（被容忍）；
                                     * 突发打满则立即触发（快速响应）。 */
#define CFG_HDR_SZ        24u       /* 每块头：len(4)+psn(4)+stamp(8)+pad(8) */

#define CFG_T_EPOCH_NS    100000000ULL  /* T_EPOCH：检查兜底周期 = 100 ms（低速率连接用） */
#define CFG_K_DWELL       2u        /* K：连续 K 个纪元同向才切换。必须 >= 2，
                                     * 保证旧池能在下次切换前排空（§2.9.4）。 */
#define CFG_J_QUIET       4u        /* J：连续 J 个纪元无溢出才允许"缩"。防抖。 */
#define CFG_SHRINK_GAMMA  0.75      /* γ：仅当 L_ring <= γ·S 才缩（缩得值才缩）。 */
#define CFG_HIST_DECAY    8u        /* 连续 8 个安静纪元后清空 ovf_max_hist。 */

#define CFG_FALLBACK_MTU  4096u     /* 建联拿不到 MTU 时的兜底初始块大小 */

/* ==================== B. 通用计时/复现 ==================== */
#define CFG_SEED          42u       /* 固定随机种子 */
#define CFG_REPS          5u        /* 每个测量重复次数（误差棒画 std） */
#define CFG_TSC_BATCH     128u      /* 批量 rdtsc 的批大小 */
#define CFG_WARMUP_OPS    200000u   /* 计时前的 warm-up 操作数 */

/* ==================== C. 实验一（时间开销） ==================== */
/* C1: 存 */
#define CFG_E1A_N                   10240u                    /* 缓存深度 */
#define CFG_E1A_PAYLOAD_LIST        { 64u, 1024u, 4096u }     /* 测哪几种包长 */
#define CFG_E1A_PAYLOAD_N           3
#define CFG_E1A_TIMED_OPS           200000u                   /* 计时多少次 store */
#define CFG_E1A_SEQ_MODE_SEQ        1u                        /* 1=顺序 PSN，0=随机 PSN */
/* C2: 取 */
#define CFG_E1B_N_LIST              { 512u,1024u,2048u,4096u,8192u,10240u }
#define CFG_E1B_N_N                 6
#define CFG_E1B_GBN_LONG            64u    /* GBN 长区间：连续取多少个包 */
#define CFG_E1B_GBN_SHORT           8u
#define CFG_E1B_SR_K1               16u    /* SR：离散取多少个包 */
#define CFG_E1B_SR_K2               64u
#define CFG_E1B_QUERIES             10000u /* 每个 (方法,模式,N) 跑多少次请求 */

/* ==================== D. 实验二（空间利用率） ==================== */
/* 【注意】从 512 B 起，不含 0–512 小包 */
#define CFG_E2_PKTSIZE_LIST  { 512u,640u,768u,1024u,1280u,1500u,1536u,2048u,3072u,4096u }
#define CFG_E2_PKTSIZE_N     10
#define CFG_E2_N_LIST        { 256u,512u,1024u,2048u,4096u,10240u }   /* 扫 N 用 */
#define CFG_E2_N_N           6
#define CFG_E2_N             10240u        /* 扫包长时固定的 N */
#define CFG_E2_FIXED_L       1024u         /* 扫 N 时固定的包长 */
#define CFG_HASH_NBUCKETS    4096u         /* 链式哈希桶数（与旧代码对齐） */
#define CFG_FIXED_BLOCK      5120u         /* psn_fixed 消融用的固定块大小 */

/* ==================== E. 实验三（动态块机制有效性）==================== */
/* ★★ 阶段表：在这里统一修改！ 格式 { 纪元数, 该阶段的包长(字节) } ★★ */
/* 默认负载：全 MTU → 全 1024（应缩）→ 突发全 4096（应扩+drain）→ 回到 1024（应缩回） */
#define CFG_E3_PHASES  { {4u,4096u}, {12u,1024u}, {2u,4096u}, {12u,1024u} }
#define CFG_E3_PHASE_N 4

/* 备用负载（可选跑法，更能体现"容忍偶发大包"）：95% @1024 + 5% @4096，跑 40 纪元。
 * 用 --e3-preset=mixed 切换。 */
#define CFG_E3_MIXED_PHASES   { {40u,0u} }   /* 纪元数, 0=表示用混合分布 */
#define CFG_E3_MIXED_SMALL    1024u          /* 混合分布里的"小包" */
#define CFG_E3_MIXED_BIG      4096u          /* 混合分布里的"大包" */
#define CFG_E3_MIXED_BIG_P    0.05           /* 大包占比 5% */

#define CFG_E3_JITTER         0.0   /* 包长抖动比例，0=固定长度。如 0.1 = ±10% 随机抖动 */
#define CFG_E3_NAK_MODE       0     /* 0=SR, 1=GBN, 2=混合（随机选） */
#define CFG_E3_NAK_RATE       0.02  /* 模拟丢包率：2% 的包"丢失"→ 触发 NAK */
#define CFG_E3_LATENCY_PROBE  0u    /* 是否在每个纪元内测取包延迟（1=测，0=不测） */

/* ==================== F. 模拟器（发包/丢包/发 NAK） ==================== */
#define CFG_SIM_LOSS_MODE     0     /* 0=均匀丢包(uniform)，1=突发丢包(Gilbert-Elliott) */
#define CFG_SIM_LOSS_RATE     0.02  /* uniform 模式的丢包率 */
#define CFG_SIM_BURST_GOOD_P  0.99  /* GE 模型：留在"好"状态的概率 */
#define CFG_SIM_BURST_BAD_P   0.98  /* GE 模型：留在"坏"状态的概率 */
#define CFG_SIM_NAK_DELAY     2u    /* 收到包后隔多少个包才发 NAK（模拟 RTT） */
#define CFG_SIM_SEED          42u   /* 模拟器随机种子（与 CFG_SEED 独立，便于复现） */

#endif
```

## 3.2 运行期覆盖 + 落盘

```c
typedef struct {
    uint32_t ring_n, n_sc, sc[16], hdr_sz;
    uint32_t ovf_cap, ovf_thresh, fallback_mtu;
    uint64_t t_epoch_ns;
    uint32_t k_dwell, j_quiet, hist_decay;
    double   shrink_gamma;
    uint32_t seed, reps, tsc_batch, warmup_ops;

    /* 实验三阶段表（运行期可覆盖） */
    uint32_t e3_phase_n;
    struct { uint32_t epochs, pkt_len; } e3_phases[16];
    double   e3_jitter, e3_nak_rate;
    uint32_t e3_nak_mode;

    /* 模拟器 */
    uint32_t sim_loss_mode, sim_nak_delay, sim_seed;
    double   sim_loss_rate, sim_burst_good_p, sim_burst_bad_p;
} cfg_t;

void cfg_default(cfg_t *c);                       /* 从 §3.1 的 #define 填默认值 */
int  cfg_override_cli(cfg_t *c, int argc, char **argv);   /* --k-dwell=3 --out=... */
int  cfg_dump_json(const cfg_t *c, const char *path);     /* resolved_config.json */
```

**每次运行必须落盘 `resolved_config.json`** —— 论文可复现性证据。

常用命令行覆盖：
```
--N=10240 --ring-n=... --ovf-cap=... --ovf-thresh=... --k-dwell=... --j-quiet=...
--shrink-gamma=... --reps=... --seed=...
--e3-phases="4:4096,12:1024,2:4096,12:1024"   --e3-nak-mode=sr --e3-nak-rate=0.02
--e3-preset=mixed                             --sim-loss-mode=burst
--out=out/exp3a
```

## 3.3 符号 ↔ 字段对照表

| 论文符号 | config 字段 | 默认值 | 含义 |
|---|---|---|---|
| `N` | `ring_n` | 10240 | 环长 / 窗口长度 |
| `S` | 运行时 `c->S` | 初始 = MTU | 当前块大小 |
| — | `sc[]` | {256…8192} | 档位集合 |
| — | `ovf_cap` | 512 | 溢出数组容量 |
| `τ_o` | `ovf_thresh` | 256 | 溢出触发扩大的条数阈值 |
| `T_e` | `t_epoch_ns` | 100 ms | 检查兜底周期 |
| `K` | `k_dwell` | 2 | 切换滞后纪元数 |
| `J` | `j_quiet` | 4 | 允许缩所需的安静纪元数 |
| `γ` | `shrink_gamma` | 0.75 | 缩的"值得"判据 |

---

# §4 单机模拟器：发包 / 丢包 / 发 NAK

> **现实约束**：目前只用**一台设备**模拟"发数据包 + 发重传请求"，**不是真实负载**。
> 所以模拟器要做的三件事：**按配置的阶段表发包** → **按模型判定丢包** → **按 RTT 延迟发 NAK**，把 NAK 交给网关处理并计时。

## 4.1 三个模块

```
sim/traffic.c   发包器：按阶段表生成 PSN 递增的数据包
sim/loss.c      丢包：判定哪些包"丢失"（uniform / Gilbert-Elliott 突发）
sim/nak.c       NAK：按 NAK_MODE 组装 GBN 区间 或 SR 集合，延迟 NAK_DELAY 后提交给网关
sim/sim_main.c  驱动：串联以上三者 + 调用 conn_store / conn_lookup + 采集指标
```

## 4.2 发包器（`sim/traffic.c`）

```c
/* 阶段表驱动的包长生成器。epoch 边界 = 每 N 个包。 */
typedef struct {
    const cfg_t *cfg;
    uint32_t psn;            /* 单调递增 */
    uint32_t phase_idx;
    uint32_t epoch_in_phase;
    uint64_t rng;
    uint32_t total_pkts;     /* 统计 */
} traffic_t;

void traffic_init(traffic_t *t, const cfg_t *cfg, int preset_mixed);
/* 生成下一个包：写 pkt_len，返回 psn */
uint32_t traffic_next(traffic_t *t, uint32_t *pkt_len) {
    if (t->phase_idx >= t->cfg->e3_phase_n) return UINT32_MAX;   /* 结束 */
    const uint32_t len = t->cfg->e3_phases[t->phase_idx].pkt_len;
    const uint32_t epochs = t->cfg->e3_phases[t->phase_idx].epochs;

    uint32_t L = len;
    if (L == 0) {   /* 0 表示"混合分布"（备用负载） */
        const double u = rng_double(&t->rng);
        L = (u < t->cfg->e3_mixed_big_p) ? t->cfg->e3_mixed_big
                                         : t->cfg->e3_mixed_small;
    }
    if (t->cfg->e3_jitter > 0.0) {          /* 可选：包长抖动 */
        const double j = 1.0 + (rng_double(&t->rng) * 2.0 - 1.0) * t->cfg->e3_jitter;
        L = (uint32_t)((double)L * j);
        if (L < 64) L = 64;
        if (L > t->cfg->sc[t->cfg->n_sc-1]) L = t->cfg->sc[t->cfg->n_sc-1];
    }
    t->psn++;
    if (++t->epoch_in_phase >= t->cfg->ring_n) {   /* 满一个纪元 */
        t->epoch_in_phase = 0;
        if (++/*该阶段已跑纪元数*/t->phase_epochs_run >= epochs) {
            t->phase_idx++; t->phase_epochs_run = 0;
        }
    }
    t->total_pkts++;
    *pkt_len = L;
    return t->psn - 1;
}
```

> `phase_epochs_run` 是 `traffic_t` 里的一个 `uint32_t` 字段（上面为节省篇幅省略了定义）。

**要点**：
- PSN **严格递增**（贴近真实连接：PSN 就是顺序分配的）。
- 阶段边界按**纪元**（= N 个包）计，与网关的检查周期**严格对齐** → 便于观察 `S` 在每个阶段的收敛。
- `e3_jitter` 可选，默认 0（固定长度，便于画阶梯图）。

## 4.3 丢包模型（`sim/loss.c`）

```c
/* 0 = uniform：每包以 loss_rate 独立"丢失"
 * 1 = Gilbert-Elliott：两状态马尔可夫链，坏状态全丢 → 模拟 WAN 突发丢包 */
typedef struct {
    const cfg_t *cfg;
    uint64_t rng;
    int bad_state;
} loss_t;

int loss_is_lost(loss_t *l, uint32_t psn) {
    (void)psn;
    if (l->cfg->sim_loss_mode == 0)
        return rng_double(&l->rng) < l->cfg->sim_loss_rate;
    /* Gilbert-Elliott */
    const double p = l->bad_state ? l->cfg->sim_burst_bad_p
                                  : l->cfg->sim_burst_good_p;
    if (rng_double(&l->rng) >= p) l->bad_state ^= 1;
    return l->bad_state;
}
```

> **语义澄清（很重要）**：丢包是**网关之后（下游）**发生的。
> 网关**已经**把这个包缓存下来了（它只是"转发过去时丢了"），所以接收端发 NAK、网关**从缓存里取**。
> 这就是网关缓存存在的意义：**丢掉的重传不必回到源端，网关就近重传。**
> 因此：**每个包都 `conn_store`（无论是否"丢"）；只有"丢"的包才出现在 NAK 里。**

## 4.4 发 NAK（`sim/nak.c`）

```c
/* 延迟 NAK_DELAY 个包后，把这段时间内"丢失"的 PSN 汇总成一个 NAK */
typedef struct {
    uint32_t *pend;          /* 待发 NAK 的 PSN 列表 */
    uint32_t  n_pend, cap;
    uint32_t  nak_epoch;     /* 用来统计 NAK 次数 */
} nakgen_t;

/* 每个包到达后调用：记录丢失；并在延迟到点时"发射"NAK */
void nak_on_packet(nakgen_t *g, const cfg_t *cfg, uint32_t psn, int lost,
                   conn_t *gw, uint8_t *outbuf, uint32_t outcap, sim_stats_t *st) {
    if (lost) push(g, psn);

    if (g->n_pend == 0) return;
    if (psn - g->first_lost_psn < cfg->sim_nak_delay) return;   /* 还没到延迟 */

    const uint64_t t0 = rdtsc();
    if (cfg->e3_nak_mode == 0) {                 /* SR：离散集合 */
        uint32_t w = conn_retransmit_set(gw, g->pend, g->n_pend, outbuf, outcap);
        st->bytes_retrans += w;
    } else if (cfg->e3_nak_mode == 1) {          /* GBN：连续区间 */
        uint32_t lo = min_of(g->pend), hi = max_of(g->pend);
        uint32_t w = conn_retransmit_range(gw, lo, hi - lo + 1, outbuf, outcap);
        st->bytes_retrans += w;
    } else {                                     /* 混合：随机 */
        if (rng_double(&g->rng) < 0.5) { /* SR 分支 */ } else { /* GBN 分支 */ }
    }
    const uint64_t t1 = rdtsc();
    st->nak_n++; st->nak_ns_total += tsc_to_ns(t1 - t0);
    /* 统计命中率：请求的 PSN 有多少还在缓存里 */
    reset_pending(g);
}
```

**关键统计（实验一的取包数据直接来自这里）**：
- `nak_n`：NAK 次数
- `nak_ns_total`：处理所有 NAK 的总耗时 → 每次 NAK 平均耗时、**每包均摊取包延迟**
- `bytes_retrans`：重传字节数
- `hit / miss`：请求 PSN 是否还在缓存（`conn_lookup` 返回 NULL 即 miss）

## 4.5 驱动（`sim/sim_main.c`）

```c
int sim_run(const cfg_t *cfg, sim_stats_t *st, trace_writer_t *tw) {
    conn_t gw; conn_init(&gw, cfg, cfg->fallback_mtu);
    traffic_t tr; traffic_init(&tr, cfg, 0);
    loss_t ls;    loss_init(&ls, cfg);
    nakgen_t ng;  nak_init(&ng, cfg);

    uint8_t *out = malloc(RETX_BUF_SZ);
    uint8_t *pkt = malloc(MAX_PKT_SZ);
    uint32_t epoch = 0;

    for (;;) {
        uint32_t len; uint32_t psn = traffic_next(&tr, &len);
        if (psn == UINT32_MAX) break;

        fill_payload(pkt, len, psn);
        conn_store(&gw, psn, pkt, (uint16_t)len);        /* 每个包都缓存 */

        int lost = loss_is_lost(&ls, psn);
        nak_on_packet(&ng, cfg, psn, lost, &gw, out, RETX_BUF_SZ, st);

        /* 纪元边界：写一行 trace（S / 利用率 / 溢出 / 延迟） */
        if ((psn + 1) % cfg->ring_n == 0) {
            trace_epoch(&gw, epoch++, cfg, tw);
        }
    }
    conn_destroy(&gw);
    free(out); free(pkt);
    return 0;
}
```

**驱动的输出（实验三的全部数据都从这里来）**：
`S_trace` / `utilization_trace` / `overflow_trace` / `latency_trace` / `overhead` / `resize_events`。

---

# §5 三个实验的完整设计

## 5.1 通用实验规范

| 项 | 规范 |
|---|---|
| 语言/构建 | C + Makefile；`gcc -O2 -Wall -std=gnu11 -lm` |
| 计时 | 批量 `rdtsc`（`cfg.tsc_batch`）+ TSC 标定到 ns |
| warm-up | 每计时循环前：填满结构 + 预热分配器 + `cfg.warmup_ops` 次空转 |
| 随机 | `xorshift32`，`seed = cfg.seed`（模拟器另用 `cfg.sim_seed`） |
| 重复 | `REPS = cfg.reps`，报告各 rep 中位数 + std |
| 输出 | `out/<exp_name>/...csv`，ASCII 列名、逗号分隔、无 BOM |
| 配置 | 每次运行落盘 `resolved_config.json` |

---

## 5.2 实验一：时间开销（存 + 取）

### 5.2.1 实验 1a —— 存（store）

**目的**：证明"把排序做进底层地址计算，**不增加**存储开销"。

**对比方法**：`fifo`、`chained_hash`、`balanced_tree`、**`psn_dynblock`（我们）**。

**自变量**：包长 ∈ `cfg.e1a_payload_list`（默认 64 / 1024 / 4096 B）；`N = cfg.e1a_n`。

**流程**：用 `2N` 次 store 填满并绕一圈（warm-up）→ 计时 `cfg.e1a_timed_ops` 次 store → 逐样本记延迟。

**输出**：
- `store_samples.csv`：`method,payload_len,seq_mode,latency_ns`
- `store_summary.csv`：`method,payload_len,seq_mode,n,mean,p25,p50,p75,p99,iqr,std`
- `malloc_per_store.csv`：`method,malloc_per_store`（**关键证据**：我们应为 0）

### 5.2.1.1 ★ 必做对照 D：**同分配策略**（已确认要做）

给 `chained_hash` / `fifo` / `balanced_tree` **各做一个 `*_pool` 变体** —— 同样从**预分配的空闲链表**取块，不再每包 malloc。
把它与 `psn_dynblock` 一起测 store。

**为什么必须做**：默认实现里，"索引结构"和"分配策略"**同时变了**（我们预分配，基线每包 malloc）→ 存在 confound。
做了对照 D 之后：
- 若 `psn_dynblock ≈ hash_pool` → **干净地证明"只换索引方式时 store 成本不变；差异来自分配策略"**。
- 若 `psn_dynblock < hash_pool` → 说明我们的**地址计算还更省**（无指针追逐）。

**输出**：`store_summary_pool.csv`（与 `store_summary.csv` 同 schema，`method` 用 `*_pool` 命名）。

### 5.2.1.2 ★ 必做对照 E：**纯索引微基准**

只计时"地址计算"本身：`psn mod N`（我们） vs 乘法哈希 vs AVL 查找路径，各 10^7 次取平均。
**输出**：`index_only.csv`：`method,ns_per_op`
**预期**：三者都是 1–2 ns 级 → 证明**映射本身零开销**。

### 5.2.1.3 图

- **Fig 1a**：箱线图。x = 4 方法；y = store 延迟（ns，**对数轴**，跨度可能 86→300+）。箱体 = IQR，须 = 5–95 分位，中位加粗。我们 = 最深蓝 + 加粗边框；基线 = 灰阶 + **不同填充纹理**（保证黑白可印）。
- **Fig 1a-补**（对照 D/E）：双子图 (a) 端到端 store 延迟（含/不含 `*_pool` 两套）；(b) 纯索引代价（近乎平线）。

**图怎么读**：① 我们中位数是否 ≤ 基线；② **IQR 是否更窄**（IQR 窄 = 延迟更可预测，对长驻网关很重要）；③ 对照 D 里我们与 `hash_pool` 是否持平（证明差异来自分配而非映射）。

---

### 5.2.2 实验 1b —— 取（retrieve）

**目的**：证明"缓存报文数量 N 增大时，其他方法变慢，我们的方法基本不变"。

**必须先定义"取"**（GBN 与 SR 都做）：

| 模式 | 定义 | 一次请求 |
|---|---|---|
| **GBN-long** | NAK 携带一段**连续** PSN 区间 | 从随机起点取 `cfg.e1b_gbn_long` 个连续包 |
| **GBN-short** | 同上，短区间 | `cfg.e1b_gbn_short` |
| **SR-16** | NAK 携带**离散** PSN 集合 | 窗口内随机取 `cfg.e1b_sr_k1` 个包 |
| **SR-64** | 同上，更多 | `cfg.e1b_sr_k2` |

**四种方法如何服务**（公平性核心，必须写清）：
- `fifo`：沿链表线性查找 → **O(N)**
- `chained_hash`：每 PSN 一次桶查找 + 指针追逐 → **O(1) 平均、高常数（cache miss）**
- `balanced_tree`：每 PSN 一次 AVL 查找 → **O(log N)**
- `psn_dynblock`：每 PSN 一次 `Φ(p)` → **O(1)**；**GBN 下这 64 个槽在内存上连续**（除回绕点）→ 顺序访问 + 预取友好

**自变量**：`N ∈ cfg.e1b_n_list`（对数横轴）。

**因变量**：每次 NAK 的**总耗时**与**每包均摊耗时**（中位数 + std）。

**流程**：填充至窗口内有 N 个包 → 按上表生成请求 → 计时"**定位 + memcpy 到发送缓冲**"全过程 → 每 (方法,模式,N) 跑 `cfg.e1b_queries` 次。

**输出**：
- `lookup_scaling.csv`：`method,mode,N,n,median_ns,median_ns_per_pkt,std`
- `lookup_cdf_<method>_<mode>_N<...>.csv`：逐样本 CDF（至少对 `N = 10240`）

**预期与诚实提醒**：
- ✅ 我们**随 N 基本平线**；`fifo` / `balanced_tree` **明显上升**。
- ⚠️ **`chained_hash` 也会基本平**（它也是 O(1) 平均）。**别说"其他方法都增加"**，准确说法是：
  **"我们的延迟随 N 保持平直且最低；`fifo`/`balanced_tree` 随 N 显著上升；`chained_hash` 也平直，但常数更高（指针追逐导致 cache miss），且 GBN 下无法顺序访问。"**
- **GBN 是杀手级证据**：我们 64 个包是**连续内存**，hash/tree 是 64 次随机访存 → 差距放大。**这张图重点做。**

**图**：
- **Fig 1b**：每包均摊延迟 vs N（**对数横轴**），marker/线型区分模式、颜色区分方法。我们最深蓝加粗。
- **Fig 1c**：CDF（`N = 10240`，SR-16 与 GBN-long 各一幅）。x = 延迟（ns，对数），y = 累计概率。图内标注 `N = 10240`。

**图怎么读**：① 线是否随 N 上翘（我们应平）；② GBN 与 SR 的差距（我们 GBN 应明显更优 → 证明"连续性"的价值）；③ CDF 长尾（我们应更靠左、尾更短）。

---

## 5.3 实验二：空间利用率

**目的**：证明我们的空间利用率**略低但差距可接受**，并解释原因。

**定义**：`utilization = payload_bytes / allocated_bytes`，`payload_bytes = N × L`。

**对比方法**：`fifo`、`chained_hash`、`balanced_tree`、`psn_dynblock`；**另加三条消融曲线**：`psn_map_static`（S 固定 = MTU）、`psn_fixed`（固定 5 KB）、`psn_map_tiered_old`（旧的 6 环方案）。

**各方法 `allocated_bytes`**（沿用旧 `space_bench.c` 模型，**不改口径**）：
- `fifo`：`N×sizeof(void*)` + `N×ALIGN16(sizeof(fifo_packet)+L)`
- `chained_hash`：`cfg.hash_nbuckets×sizeof(void*)` + `N×(ALIGN16(node)+ALIGN16(HDR+L))`
- `balanced_tree`：`N×(ALIGN16(avl_node)+ALIGN16(HDR+L))`
- `psn_dynblock`：`N×ALIGN16(HDR+S*)` + `N×sizeof(slot_meta_t)` + `ovf_bytes`
  - 同质包长 L 的稳态：`S* = ceil_class(L)`、溢出为空 → 可直接算出，**无需跑模拟**。
  - 亦可跑真实自适应循环取稳态占用（复用 §4 代码）。
- `psn_map_static`：`N×ALIGN16(HDR+MTU)` + `N×sizeof(slot_meta_t)`
- `psn_fixed`：`N×ALIGN16(5120)`
- `psn_map_tiered_old`：用旧公式（6 环 + 标记数组）

**自变量**：包长 ∈ `cfg.e2_pktsize_list`（**从 512 起**）；另加 `N ∈ cfg.e2_n_list` 一维扫描。

**输出**：
- `space_vs_pktsize.csv`：`method,L,N,payload_bytes,allocated_bytes,utilization,avg_bytes_per_pkt,S_current`
- `space_vs_N.csv`：`method,N,L,payload_bytes,allocated_bytes,utilization`

**预期**：
- `fifo` 最高；我们**略低于 fifo、与 hash/tree 相当**；
- 我们**远高于 `psn_map_static`**（L=1024 时 static ≈ 25%，我们 ≈ 98%）→ **这就是自适应机制的价值**；
- `psn_map_tiered_old` 在 **小 N** 时崩塌（固定开销大），我们的单环不会。

**图**：
- **Fig 2a**：利用率 vs 包长（4 主 + 3 消融）。x 从 512 起。图例放**图内**。
- **Fig 2b**：利用率 vs N（固定 L = `cfg.e2_fixed_l`）。**专门展示"固定开销"**：旧 6 环方案小 N 时暴跌，我们平稳。

**图怎么读**：① 我们是否贴近 fifo/hash（"差距可接受"）；② Fig 2a 中我们**高于** static（自适应收益）；③ Fig 2b 中我们**不随 N 崩塌**（单环 vs 6 环）。

---

## 5.4 实验三：动态块机制的**有效性**

**目的**：用简单、自包含的实验，证明自适应机制**真的在工作**且**代价可忽略**。

### 5.4.1 负载：分阶段变化（从 `cfg.e3_phases` 读）

默认阶段表（**可在 `config.h` 里统一改**）：

| 阶段 | 纪元数 | 包长 | 期望行为 |
|---|---|---|---|
| **P1** | 4 | 4096（= MTU） | `S` 维持 4096，溢出 = 0 |
| **P2** | 12 | 1024 | **缩**到 1024；利用率从 ~25% 跳到 ~98% |
| **P3** | 2 | 4096 | 大包涌入 S=1024 的环 ⇒ **溢出被填** ⇒ 达阈值 ⇒ **扩**回 4096 ⇒ **drain 清空溢出** |
| **P4** | 12 | 1024 | 安静若干纪元后**缩**回 1024（验证迟滞与护栏） |

**备用负载 `--e3-preset=mixed`**：95% @1024 + 5% @4096，跑 40 纪元 → 看 `S` 是否稳住 1024 且溢出保持低位（说明阈值正确地把"偶发大包"留在溢出）。

### 5.4.2 每纪元记录

`epoch, phase, S_after, event, L_ring, ovf_count, L_ovf, util_adaptive, util_static, lookup_p50_ns, check_ns, resize_ns`

### 5.4.3 同时证明"代价可忽略"

1. **检查开销**：每纪元 `check_and_maybe_resize` 耗时（应 ~1 μs，相对一整圈 N 个包可忽略）；
2. **切换开销**：每次 `resize` + `drain` 耗时（应 O(1)+O(OVF_CAP)，微秒级）；
3. **切换期间转发延迟**：纪元内取包 P50（应**无尖峰** → 证明"不停世界"）。

### 5.4.4 输出

- `S_trace.csv`：`epoch,phase,S_after,event,L_ring,ovf_count,L_ovf`
- `utilization_trace.csv`：`epoch,phase,util_adaptive,util_static`
- `overflow_trace.csv`：`epoch,phase,ovf_count,ovf_bytes`
- `latency_trace.csv`：`epoch,phase,lookup_p50_ns,lookup_p99_ns`
- `overhead.csv`：`epoch,check_ns,resize_ns,drain_ns,moved_pkts`
- `resize_events.csv`：`epoch,S_old,S_new,reason,moved_pkts`

### 5.4.5 图

- **Fig 3（双子图）**：
  - **(a)** `S` vs `epoch` 阶梯图 + **阶段底色分区**（P1–P4）+ 标出缩/长触发点；
  - **(b)** `utilization` vs `epoch`：**adaptive（深蓝实线）** vs **static-MTU（灰虚线）**。
- **Fig 3-补**（版面允许或放补充材料）：`ovf_count` vs `epoch` + `lookup_p50` vs `epoch`（平线 = 不停世界）。

**图怎么读**：
- (a) `S` 是否**在第 2–3 个纪元内收敛**到正确档位、P3 是否**及时扩回**、P4 是否**在迟滞结束后缩回**（不抖动 = 护栏有效）；
- (b) 两条曲线的**面积差** = 自适应带来的空间节省（"机制有用"的最直观证据）；
- 补充面板：`ovf_count` 是否**有界且峰值低**、`lookup_p50` 是否**无尖峰**。

### 5.4.6 （可选）超参敏感性

扫 `K ∈ {1,2,4,8}` × `OVF_THRESH ∈ {64,128,256,512}`，看 4 个指标：收敛纪元数、稳态利用率、切换次数（越少越好）、`ovf_count` 峰值。
输出 `sensitivity.csv`。**放补充材料**，能把"参数不是乱选的"变成加分点。

---

## 5.5 三种负载的"预期结果"速查（供验收对照）

| 观察 | 期望 |
|---|---|
| `S` 收敛速度 | P2 开始后 **≤ 3 个纪元**内落到 1024 |
| 利用率提升 | P2 稳态 adaptive ≈ 95–98%，static ≈ 25% |
| 溢出峰值 | P3 期间 ≤ `OVF_CAP`；P3 结束后很快归零（drain 生效） |
| 切换次数 | 整段（30 纪元）**≤ 6 次**（不抖动） |
| `lookup_p50` | 全程平直，**无尖峰** |
| 池峰值 | ≤ 2 |

---

## 5.6 出图规范（统一）

| 项 | 规范 |
|---|---|
| 格式 | **PDF 矢量**（投稿）+ PNG 300 dpi（预览） |
| 字体 | serif / Times；字号 **8–9 pt**；不要用默认 DejaVu |
| 配色 | 灰度可读优先。**我们 = 最深蓝 + 加粗 + 独有 marker**；基线 = 灰阶 |
| 黑白友好 | 除颜色外必须有**第二视觉通道**（marker 形状 / 线型 / 填充纹理） |
| 图例 | Fig 2a 图例放**图内**；Fig 1b 图例放**右下角** |
| 坐标轴 | 延迟类用**对数轴**；利用率用 0–100% 线性 |
| 标注 | CDF 图必须标 `N = 10240`；箱线图必须标口径（payload 长度、稳态、log scale） |
| 命名 | `fig1a_store_box` / `fig1b_lookup_scaling` / `fig1c_lookup_cdf` / `fig2a_space_vs_pktsize` / `fig2b_space_vs_N` / `fig3_adaptive`（各有 `.pdf` 与 `.png`） |
| 脚本 | `paper_figures/plot_all.py --exp=all --out=figures/` 一键出全部图 |

```python
import matplotlib
matplotlib.rcParams.update({
    "font.family": "serif",
    "font.serif": ["Times New Roman", "Nimbus Roman", "DejaVu Serif"],
    "font.size": 8.5, "axes.linewidth": 0.6,
    "pdf.fonttype": 42, "ps.fonttype": 42,   # PDF 内嵌可编辑字体
    "savefig.bbox": "tight",
})
```

---

# §6 目录结构与运行命令

## 6.1 目录（全部在 `260916_cache_AddExperiment1/` 下）

```
260916_cache_AddExperiment1/
├── include/
│   ├── config.h          # §3.1 【唯一配置文件：机制 + 全部实验预设值】
│   └── dynblock.h        # conn_t 等结构 + 接口声明
├── src/
│   ├── config.c          # cfg_default / cfg_override_cli / cfg_dump_json
│   ├── dynblock.c        # ★ 新算法：init/store/lookup/check/gen_switch/drain/emergency
│   ├── pool.c            # pool_alloc/free + 溢出 arena（bump + 空闲链表）
│   └── util.c            # align16/ceil_class/rdtsc 标定/xorshift32/CSV 写出
├── sim/
│   ├── traffic.c         # §4.2 发包器
│   ├── loss.c            # §4.3 丢包模型
│   ├── nak.c             # §4.4 发 NAK
│   └── sim_main.c        # §4.5 驱动
├── bench/
│   ├── exp1a_store.c
│   ├── exp1b_lookup.c
│   ├── exp2_space.c
│   └── exp3_adaptive.c
├── baselines/            # 从 260912 复制来的 fifo/chained_hash/balanced_tree
│                         #   + 新增 *_pool 变体（对照 D）
│                         #   + psn_fixed / psn_map_static / psn_map_tiered_old（消融）
├── paper_figures/
│   └── plot_all.py
├── out/                  # 全部 CSV
├── figures/              # 全部图（pdf + png）
├── Makefile
├── README.md             # 编译、运行、复现命令
├── RESULTS.md            # ★ 结果报告
└── ASSERTIONS.txt        # ★ §2.12 自测输出
```

## 6.2 一键复现

```bash
cd 260916_cache_AddExperiment1
make -j

# 自测（先跑这个！）
./bench/selftest                      # 产出 ASSERTIONS.txt

# 实验一
./bench/exp1a_store  --N=10240 --reps=5 --out=out/exp1a
./bench/exp1b_lookup --N-list=512,1024,2048,4096,8192,10240 --out=out/exp1b

# 实验二
./bench/exp2_space   --N=10240 --N-list=256,512,1024,2048,4096,10240 --out=out/exp2

# 实验三（默认阶段表来自 config.h；也可命令行覆盖）
./bench/exp3_adaptive --out=out/exp3
./bench/exp3_adaptive --e3-preset=mixed --out=out/exp3_mixed

# 出图
python3 paper_figures/plot_all.py --exp=all --out=figures/
```

---

# §7 验收清单（交付前逐条自查）

- [ ] 新目录 `260916_cache_AddExperiment1/` **自包含可编译**（`make -j` 一把过）。
- [ ] **旧目录 `260912_cache_ModifyExperiment/` 一个文件都没被改动。**
- [ ] §2.12 的 A1–A10 全部 pass，写入 `ASSERTIONS.txt`。
- [ ] 模拟器能按 `cfg.e3_phases` 发包、按丢包模型丢包、按延迟发 NAK（SR 与 GBN 两种都跑通）。
- [ ] `psn_dynblock` 在 N 从 512 到 10240 时，**取包延迟基本为平线**。
- [ ] `psn_dynblock` 的 store P50 **不差于 `chained_hash`**，且 **IQR 更窄**。
- [ ] **对照 D 已完成**（`*_pool` 变体），数据在 `store_summary_pool.csv`。
- [ ] **对照 E 已完成**（`index_only.csv`）。
- [ ] 稳态 `malloc/store == 0`（环路径），`malloc_per_store.csv` 已产出。
- [ ] 空间利用率：L=1024 时 **≥ 90%**（static 方案约 25%，对比鲜明）。
- [ ] 空间利用率曲线**从 512 B 起画**，无旧 tiered 在 1280 B 处的凹陷。
- [ ] 实验三 `S` 在 P2/P3/P4 **按预期收敛**，P4 **不抖动**；`lookup_p50` **无尖峰**。
- [ ] 全部 CSV 与图**一键复现**（README 给出确切命令）。
- [ ] 每次运行都落盘 `resolved_config.json`。
- [ ] `RESULTS.md` 给出每实验一句话结论 + 关键数字表 + 与旧 `psn_tiered` 的差异说明。
